{{- if .Values.analyticsService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openbiocure-platform.fullname" . }}-analytics-service
  labels:
    {{- include "openbiocure-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: analytics-service
spec:
  {{- if not .Values.analyticsService.autoscaling.enabled }}
  replicas: {{ .Values.analyticsService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "openbiocure-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: analytics-service
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/analytics-service-configmap.yaml") . | sha256sum }}
      labels:
        {{- include "openbiocure-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: analytics-service
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "openbiocure-platform.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: analytics-service
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.analyticsService.image.repository }}:{{ .Values.analyticsService.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.analyticsService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.analyticsService.service.targetPort }}
              protocol: TCP
          env:
            {{- range .Values.analyticsService.env }}
            - name: {{ .name }}
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            - name: PYTHONPATH
              value: "/app"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.analyticsService.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
