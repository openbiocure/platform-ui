apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openbiocure-platform.fullname" . }}-test-connection"
  labels:
    {{- include "openbiocure-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openbiocure-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
